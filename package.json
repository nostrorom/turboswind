{
  "name": "turboswind",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "build": "FORCE_COLOR=1 turbo run build",
    "dev": "FORCE_COLOR=1 turbo run dev",
    "lint": "FORCE_COLOR=1 turbo run lint",
    "format": "FORCE_COLOR=1 turbo run format",
    "format:root": "prettier --write .",
    "bump": "ncu -u && FORCE_COLOR=1 turbo run bump"
  },
  "devDependencies": {
    "@turboswind/eslint-config": "workspace:*",
    "@turboswind/prettier": "workspace:*",
    "husky": "^8.0.3",
    "prettier": "^2.8.7",
    "prettier-plugin-svelte": "^2.10.0",
    "turbo": "^1.9.1"
  },
  "packageManager": "pnpm@8.2.0"
}
