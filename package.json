{
  "name": "turboswind",
  "private": true,
  "scripts": {
    "preprate": "husky install",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format:root": "prettier --write .",
    "bump": "ncu -u && turbo run bump"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "^2.8.7",
    "prettier-plugin-svelte": "^2.10.0",
    "turbo": "^1.8.5",
    "@turboswind/eslint-config": "workspace:*",
    "@turboswind/prettier": "workspace:*"
  },
  "packageManager": "pnpm@7.15.0"
}
